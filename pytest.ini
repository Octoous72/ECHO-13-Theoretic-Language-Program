# pytest.ini
[pytest]
minversion = 6.0
addopts = -ra -q
testpaths = tests
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    network: tests that require network access
    conda: tests that require a specific conda environment or packages
